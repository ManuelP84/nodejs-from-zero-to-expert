const name = "Deadpool";
const actual = "Wade Wingston";

const normal = name + " " + actual;
const template = `${name} ${actual}`;

console.log(template);

const html = `
<h1>Hello</h1>
<p>World!</p>
`;

console.log(html);
